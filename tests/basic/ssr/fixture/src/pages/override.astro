---
import { setPagemeta } from "@grepco/astro-pagemeta/runtime";

// First call sets initial metadata
setPagemeta(Astro, {
    author: "Initial Author",
    description: "Initial description",
    title: "Initial Title"
});

// Second call merges: overrides title, keeps author, adds new field
setPagemeta(Astro, {
    description: "Overridden description",
    name: "Site Name",
    title: "Overridden Title"
});
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
    </head>
    <body>
        <h1>Multiple setPagemeta Calls</h1>
        <p>
            Tests that subsequent calls merge metadata, overriding on conflict.
        </p>
    </body>
</html>
